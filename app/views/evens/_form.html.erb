<%= form_for(@even) do |f| %>
  <% if @even.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@even.errors.count, "error") %> prohibited this even from being saved:</h2>

      <ul>
      <% @even.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :number %><br>
    <%= f.number_field :number %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
